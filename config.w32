// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("jansson", "for jansson support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("jansson", "enable jansson support", "no");

if (PHP_JANSSON != "no") {
	EXTENSION("jansson", "jansson.c");
}

